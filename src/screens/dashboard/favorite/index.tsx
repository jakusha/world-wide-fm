import { Screen } from "@shared/Screen";
import { Text, View } from "react-native"

const Favorite = () => {
    return <Screen>

        <Text>Favorite</Text>
    </Screen>
}

export default Favorite;